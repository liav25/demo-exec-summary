{
  "name": "ai-security-analyst",
  "version": "1.0.0",
  "description": "AI-powered security report generation with React frontend and FastAPI backend",
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "frontend": "cd frontend && npm run dev",
    "backend": "cd backend && python -m uvicorn main:app --host 0.0.0.0 --port 8000 --reload",
    "install-all": "cd frontend && npm install && cd ../backend && pip install -r requirements.txt",
    "build": "cd frontend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": ["AI", "security", "reports", "React", "FastAPI", "cybersecurity"],
  "author": "Your Name",
  "license": "MIT"
} 